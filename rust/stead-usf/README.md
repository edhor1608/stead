# stead-usf

Universal Session Format (USF) abstractions for normalizing AI CLI sessions into a single schema.

Intended use:
- adapter implementations per CLI/runtime
- cross-tool session browsing and orchestration layers
- portable integration beyond the Stead control room
