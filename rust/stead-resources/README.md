# stead-resources

Resource coordination primitives for agent-driven workflows:
- claim/release semantics
- conflict detection surface
- deterministic negotiation hooks

This crate is scoped as a reusable domain library, not tied to a specific UI.
